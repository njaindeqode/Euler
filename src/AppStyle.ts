import { Paper, Box } from '@mui/material'
import { styled } from '@mui/material/styles'

export const CustomPaper = styled(Paper)({ height: '100%', paddingTop: '30px' })

export const ComponentUserContainer = styled(Box)({ display: 'flex', justifyContent: 'center', marginBottom: '30px' })

export const ComponentSwapContainer = styled(Box)({ display: 'flex', justifyContent: 'center' })

export const DivContainer = styled(Box)({ minHeight: '100vh' })
